# ─── Phase 0: minimal FastAPI stack ─────────────────────────────
# Web framework & ASGI server
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Typed settings from .env
pydantic-settings==2.7.1

# ─── Phase 1: PostgreSQL (Neon) async stack ─────────────────────
# Async ORM/engine; [asyncio] enables AsyncEngine, AsyncSession
sqlalchemy[asyncio]==2.0.36

# Async PostgreSQL driver used by the sqlalchemy+asyncpg dialect
asyncpg==0.30.0

# Required runtime dependency for sqlalchemy's async bridge
greenlet==3.1.1

# ─── Phase 2: Schema migrations + vector type ───────────────────
# Alembic: DB migration tool (like EF Migrations for Python)
alembic==1.14.0

# pgvector Python client: SQLAlchemy Vector() column type that maps
# to PostgreSQL's VECTOR(n). Required so autogenerate emits VECTOR
# columns and HNSW index DDL correctly.
pgvector==0.3.6

# ─── Phase 3+ will append: ──────────────────────────────────────
# pypdf, python-docx, openpyxl, pandas, pillow, pytesseract
# openai, tiktoken
# PyJWT, httpx, python-multipart

